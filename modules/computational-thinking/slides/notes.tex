\documentclass[a4paper,10pt,oneside,openright,
               oldfontcommands]{memoir}
\input{preamble.tex}

\usepackage[noamsthm,notheorems]{beamerarticle}
%\setjobnamebeamerversion{slides}

% contents.tex is woven from contents.nw; noweb.sty defines the chunk
% markup macros the weave emits.  The [minted] option makes the chunks
% render through minted (the weave runs tominted).
\usepackage[minted]{noweb}
\noweboptions{breakcode}
% noweb's identifier index (the "Definierar:/Använder:" lists under each
% chunk) only recognises ASCII identifiers: autodefs.python and finduses
% never see ägg, mjölk, mjöl or smör, so the lists came out half-empty and
% read as errors.  Hide them in this deck until the noweb fork is
% UTF-8-aware (dbosk/noweb#13); the chunk cross-references
% (sub-page tags, "used in") stay.
\def\nwidentdefs#1{}
\def\nwidentuses#1{}

\begin{document}
\title{%
  Algoritmiskt tänkande
}
\author{Daniel Bosk}
\institute{%
  KTH EECS
}

\maketitle

\begin{abstract}
  \input{abstract.tex}
\end{abstract}

\clearpage

\tableofcontents*

\chapter{Algoritmiskt tänkande}

\input{contents.tex}

\appendix
% Treat appendix chapters as the "appendix" cref type; its Swedish name
% ("bilaga") is set with the other cref names in preamble.tex.
\crefalias{chapter}{appendix}
\input{sokprotokoll.tex}

\printbibliography
\end{document}
