\documentclass[a4paper,10pt,oneside,openright,
               oldfontcommands]{memoir}
\input{preamble.tex}

\usepackage[noamsthm,notheorems]{beamerarticle}

% contents.tex is woven from contents.nw; noweb.sty defines the chunk
% markup macros the weave emits.  The [minted] option makes the chunks
% render through minted (the weave runs tominted).
\usepackage[minted]{noweb}
\noweboptions{breakcode}
% noweb's identifier index (the "Definierar:/Använder:" lists under each
% chunk) only recognises ASCII identifiers and indexes the tangled data
% file with the same machinery as the Python chunks, so the lists come
% out inconsistent and read as errors.  Hide them until the noweb fork is
% UTF-8-aware (dbosk/noweb#13); the chunk cross-references (sub-page
% tags, "used in") stay.
\def\nwidentdefs#1{}
\def\nwidentuses#1{}

\begin{document}
\title{%
  Arbeta med filer
}
\author{Daniel Bosk}
\institute{%
  KTH EECS
}

\maketitle

\begin{abstract}
  \input{abstract.tex}
\end{abstract}

\clearpage

\tableofcontents*

\chapter{Arbeta med filer}

\input{contents.tex}

\appendix
% Treat appendix chapters as the "appendix" cref type; its Swedish name
% ("bilaga") is set with the other cref names in preamble.tex.
\crefalias{chapter}{appendix}
\input{sokprotokoll.tex}

\printbibliography
\end{document}
