notes.pdf
slides.pdf
contents.tex
examples/
# PythonTeX (didactic's \runpython) writes these next to the sources
didactic_output_*.txt
*.pytxcode
pythontex-files-*
# tangled from the makefiles submodule's tex.mk.nw by the build
latexmkrc
# Mentipy: the virtualenv PythonTeX imports mentipy from, the question
# store (which also holds the students' answers) and the generated QR
# codes.  Recreate the first with `mentipy init --venv .venv`.
.venv/
mentipy.json
mentipy-obj/
