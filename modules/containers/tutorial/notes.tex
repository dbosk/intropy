\documentclass[a4paper,10pt,oneside,openright,
               oldfontcommands]{memoir}
\input{preamble.tex}

\usepackage[noamsthm,notheorems]{beamerarticle}

% contents.tex is woven from contents.nw; noweb.sty defines the chunk
% markup macros the weave emits.  The [minted] option makes the chunks
% render through minted (the weave runs tominted).
\usepackage[minted]{noweb}
\noweboptions{breakcode}
% noweb's identifier index (the "Defines:/Uses:" lists under each chunk)
% only recognises ASCII identifiers, and it indexes chunks of every
% language with the same machinery, so the lists come out inconsistent and
% read as errors.  Hide them until the noweb fork is UTF-8-aware
% (dbosk/noweb#13); the chunk cross-references (sub-page tags, "used in")
% stay -- they are the point of the literate notes.
\def\nwidentdefs#1{}
\def\nwidentuses#1{}

\begin{document}
\title{%
  Övning: Fler behållare och mer om klasser
}
\author{Daniel Bosk}
\institute{%
  KTH EECS
}

\maketitle

\begin{abstract}
  \input{abstract.tex}
\end{abstract}

\clearpage

\tableofcontents*

\chapter{Övning: Fler behållare och mer om klasser}

\input{contents.tex}

\appendix
% PROJECT: language-specific.  Treat appendix chapters as the "appendix"
% cref type; its localised name is set with the other cref names in
% preamble.tex.
\crefalias{chapter}{appendix}
\input{sokprotokoll.tex}

\printbibliography
\end{document}
